Hi! I hope you're doing well.

I've been trying to solve this problem for almost two days now, and honestly I'm starting to think I'm looking at it completely the wrong way.

I'm working on a Rust application that processes a large number of log files. The overall architecture is actually working exactly as I intended, but there are a few issues that I simply cannot seem to understand.

The application scans a directory, reads every file, parses each line, extracts structured information, stores it into memory, performs some analysis, and finally exports a JSON report.

Everything compiles successfully.

The problem is that after processing around five thousand files the memory usage starts increasing dramatically until eventually the operating system kills the process.

I don't really know whether this is actually a memory leak, whether I'm accidentally cloning data somewhere, whether I'm holding references longer than I should, or whether my data structures are simply inefficient.

Here's the parser.

use std::fs;

pub fn parse(path: &str) -> Result<Vec<String>, std::io::Error> {
    let data = fs::read_to_string(path)?;
    Ok(data.lines().map(|s| s.to_string()).collect())
}

Here's how I'm calling it.

fn main() {
    let logs = parse("logs.txt").unwrap();

    analyze(logs);
}

The logs look something like this.

INFO Connected to server
INFO Download complete
WARNING Retry attempt 1
WARNING Retry attempt 2
ERROR Connection lost

I'm running the application using

cargo run --release

The panic looks like this.

thread 'main' panicked at src/main.rs:42:17:
called `Result::unwrap()` on an `Err` value

Backtrace

0: rust_begin_unwind
1: core::panicking::panic_fmt
2: core::result::unwrap_failed
3: parser::main

I have already tried all of the following:

- restarting my computer,
- rebuilding the project,
- deleting Cargo.lock,
- updating every dependency,
- reinstalling Rust,
- checking Stack Overflow,
- reading the Rust book,
- searching Reddit,
- asking two friends,
- adding println! statements literally everywhere,
- using cargo clean,
- trying different input files,
- reducing the file size,
- changing optimization levels,
- switching from Vec<String> to Vec<&str>,
- profiling with perf,
- profiling with valgrind,
- profiling with heaptrack.

Unfortunately none of those things really helped me understand what's going on.

What I'm hoping you can do is:

- explain exactly why this panic happens,
- identify every possible memory problem,
- identify ownership mistakes,
- explain whether unnecessary cloning is happening,
- explain whether my parser design is appropriate,
- recommend better data structures,
- suggest architectural improvements,
- preserve the current design as much as possible,
- avoid rewriting the whole application,
- explain everything step by step because I'm still learning Rust,
- and please explain the reasoning behind every recommendation instead of only giving code because I genuinely want to understand what I'm doing wrong.

I would really appreciate any help because I've spent almost an entire weekend on this one problem and I'm starting to feel like I'm just missing something obvious.
